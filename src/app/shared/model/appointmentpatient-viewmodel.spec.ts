import { AppointmentpatientViewmodel } from './appointmentpatient-viewmodel';

describe('AppointmentpatientViewmodel', () => {
  it('should create an instance', () => {
    expect(new AppointmentpatientViewmodel()).toBeTruthy();
  });
});
